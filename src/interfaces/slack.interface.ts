import { ChannelType } from '@/enums';

export interface SlackMessage {
  // id: number;
  name: string;
  description: string;
  clusterName: string;
  severity: string;
  // updatedBy: string;
  // updatedAt: Date;
  // createdBy: string;
  // createdAt: Date;
}
