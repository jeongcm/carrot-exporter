import bcrypt from 'bcrypt';
import DB from '@/database';
import { HttpException } from '@/common/exceptions/HttpException';
import { User } from '@/common/interfaces/users.interface';
import { isEmpty } from '@/common/utils/util';
import config from 'config';
import urlJoin from 'url-join';

class ProductCatlogService {
//   public catalogPlan = DB.catalogPlan;

//   public async findAllProductPlan(): Promise<User[]> {
//     const allUser: User[] = await this.catalogPlan.findAll();
//     return allUser;
//   }

 
}

export default ProductCatlogService;
